
namespace PathfindingPCL
{
    public interface IHM
    {
        void PrintResult(string _path, double _distance);
		// afficher le résultat fourni sous la forme d’un chemin et de la distance obtenue pour le chemin.
    }
}
